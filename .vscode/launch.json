{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run price parser",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/run_price_parser.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}